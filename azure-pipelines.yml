trigger: none

pool:
  name: Default

steps:
- script: |
    python --version
    echo "Simulando instalación de dependencias..."
  displayName: 'Verificar Python y simular instalación'

- script: |
    echo "Ejecución de tests..."
    echo "✅ Todo pasó correctamente"
    exit 0
  displayName: 'Simular tests'
